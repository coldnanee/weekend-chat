import SupportPage from "@/pages/support-page";

import { createMetadata } from "@/shared";

export const metadata = createMetadata("Support");

export default function Support() {
	return <SupportPage />;
}
