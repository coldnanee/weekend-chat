import "@testing-library/jest-dom";
// import { render, screen, waitFor } from "@testing-library/react";
// import LazyLoginPage from "./ui";

describe("LoginPage", () => {
	test("render", async () => {
		// render(<LazyLoginPage />);
		// await waitFor(() => {
		// 	expect(screen.getByTestId("login-page")).toBeInTheDocument();
		// });
	});
});
