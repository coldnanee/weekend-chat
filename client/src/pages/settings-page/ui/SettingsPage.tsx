import { Settings } from "@/widgets/settings";

import "./index.scss";

const SettingsPage = () => {
	return (
		<div data-testid="settings-page">
			<Settings />
		</div>
	);
};

export default SettingsPage;
